# Where to write swiss-release.apk (defaults to the project root)
OUTPUT_DIR=./dist

# Optional: provide your own release keystore.
# If omitted, a temporary self-signed one is generated.
# KEYSTORE_FILE=/path/to/release.jks
# KEY_ALIAS=my-key
# KEY_PASSWORD=secret
# STORE_PASSWORD=secret
